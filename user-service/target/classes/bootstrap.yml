spring:
  application:
    name: user-service
  profiles:
    active: default
  cloud:
    stream:
      bindings:
        output:
          destination:  userCreateTopic
          content-type: application/json
      kafka:
        binder:
          zkNodes: localhost
          brokers: localhost
    config:
      uri: http://config:8888